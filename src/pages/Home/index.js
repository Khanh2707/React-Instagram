import classNames from 'classnames/bind';
import styles from './Home.module.css';

const cx = classNames.bind(styles)

function Home() {

    return (
        <h1 style={{marginLeft: '300px'}}>Home Page</h1>
    )
}

export default Home;